services:
  - type: web
    name: lending-risk-intelligence
    runtime: python
    buildCommand: "echo 'Static site - no build required'"
    startCommand: "python3 -m http.server 8000"
    envVars:
      - key: PYTHONUNBUFFERED
        value: true
